---
# list of admission plugins that needs to be configured
# kube_apiserver_admission_plugins_needs_configuration: [EventRateLimit, PodSecurity]
etcd_events_access_addresses_semicolon: "{{etcd_clusters['k8s-events'].etcd_access_addresses_semicolon}}"
etcd_access_addresses: "{{ etcd_clusters['k8s'].etcd_access_addresses}}"
kubeadmConfig_api_version: v1beta4
